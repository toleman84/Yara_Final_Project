loki:
  url: "http://loki:3100"
  labels:
    job:
      - postfix
      - scanner
      - sigma_scanner

# How often (seconds) to poll Loki
fetch_interval: 60

# Where to find your pre-compiled LogQL queries
compiled_dir: "compiled"
